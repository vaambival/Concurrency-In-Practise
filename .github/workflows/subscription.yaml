name: subscription-service
on:
  push:
    branches:
      - master
  jobs:
    build:
      timeout-minutes: 10
      runs-on: ubuntu-latest
      steps:
        - name: Checkout
          uses: actions/checkout@v2